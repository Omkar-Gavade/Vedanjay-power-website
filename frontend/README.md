# Frontend — pending scaffold

React + Vite + Tailwind CSS.

**Not yet scaffolded.** The architecture is fully specified in
[`docs/04-architecture/frontend-architecture.md`](../docs/04-architecture/frontend-architecture.md) —
directory structure, component boundaries, routing, data flow, state management, API layer,
error handling, accessibility strategy, performance strategy and SEO implementation.

Design tokens and the component inventory are specified in [`docs/03-design/`](../docs/03-design/).

Per the brief, implementation begins only after the design direction is approved and the
launch-blocking `TO VERIFY` items are resolved.
