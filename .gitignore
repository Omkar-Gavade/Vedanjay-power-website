node_modules/
dist/
build/
.env
.env.local
*.log
.DS_Store
.vite/
coverage/
playwright-report/

# Local Worker secrets — never committed
.dev.vars

.cache/

# Local Miniflare/Wrangler runtime state — caches, rate-limit counters and
# observability traces from `wrangler dev`. Machine-local, regenerated on demand.
.wrangler/
